version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm ci
      - npm run build
run:
  runtime-version: nodejs16
  command: npm start
  network:
    port: 3000
    env: NODE_ENV=production